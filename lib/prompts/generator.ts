/**
 * Question/task generator system prompts (Phase 1).
 *
 * Generation runs on the Flash tier at high temperature with Search
 * grounding enabled (see lib/gemini/models.ts for routing).
 */

export const GENERATOR_PROMPTS_PLACEHOLDER = true;
